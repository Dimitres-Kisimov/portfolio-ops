# Human input queue

Things only Dimitres can do (a GitHub login, a GUI app, or a personal decision).
The improvement routine appends here; the full standing list lives in
`../PORTFOLIO_NEXT_STEPS.md`.

## Now
- [ ] Accept the pending GitHub **transfer invitations** on `Dimitres-Kisimov`
      (https://github.com/notifications) as new repos land.
- [ ] (Optional) Create a classic PAT on `Dimitres-Kisimov` (scopes `repo`,
      `workflow`) and share it so pushes go direct and skip the transfer step.

## This week
- [ ] Power BI: import `revops-optimizer/powerbi/data/*.csv`, add the DAX measures,
      build the 3 pages, screenshot into `revops-optimizer/docs/img/`.
- [ ] Screenshots of each dashboard / web page / deliverable into that repo's
      `docs/img/`.
- [ ] Pin the best 4–5 repos; set each repo's About + topics; add a profile README.

## Decisions
- [ ] Scheduler for the 3-hourly check + daily improvement: **local Task Scheduler**
      or **cloud routine**?
- [ ] Own-data model: OK to use the SCS repo's committed public data, or provide
      other non-confidential data?
